# 2026-09-09 工作日志

## 推送收尾（09:00-09:10）
- 用户说"现在可以直接推了"。先探测发现**本机到 GitHub 的连通性已恢复**（`git ls-remote origin main` 成功），与昨天记录的"完全不可达"不同 → 判定为**间歇性**故障，已修正用户级记忆，要求以后每次推送前先探测 10 秒。
- 推送：`git push origin main` 直接成功（走 GCM 缓存凭据，**无需手工拼 token**；`schannel: server closed abruptly` 类报错属瞬时抖动，重试 1-2 次即可）。
- 提交 `a5e1a42`（仅 2026-09-08 的工作日志文档），二进制无变化 → **服务器无需重新部署**。
- 最终三方一致：GitHub main = 本地 main = 本地 origin/main = `a5e1a42`，`git status -sb` 干净。
- **本机 git 引用读取异常（老问题复发）**：`git fetch` 日志明明显示 `4513071..a5e1a42 main -> origin/main`，但随后 `git rev-parse origin/main` 仍返回 `packed-refs` 里的陈旧值 `4513071`，且 loose ref 被 fetch 删除。解决：`git rev-parse main > .git/refs/remotes/origin/main` 直写 loose ref。教训：**验证远端状态一律用 `git ls-remote`（权威，直接问服务器），不要信本地 `origin/main`**。

## 服务器巡检中发现的既有问题（待用户决定是否排查）
- 运行中：PID 249777，二进制 sha256 `b069327a...`（= 提交 67f0137 构建），HTTP 200，启动于 2026-09-08 13:07 UTC。
- **WS_PROTOCOL 错误**：`ws dial: WS_PROTOCOL upstream 0: ws read before completion: websocket: RSV2 set, bad opcode 12`。
  - 近 24h **243 次**，同期请求 1898 → 约 12.8%；502 共 220 次。
  - 分布：9/8 01:00 UTC 集中爆发 116 次，之后每小时 1-31 次；9/9 00:00 有 3 次。
  - **早于我们所有改动**（首次 9/8 01:13:22，PID 238418，是昨天部署前的旧进程）→ 非本次引入。
  - 涉及账号分散（单账号最多 11 次），非单账号故障。
- 初步判断：帧首字节 `RSV2|opcode12` = 0x4C = ASCII 'L'，疑似上游在握手后返回了**非 WebSocket 的内容**（如限流文本响应）被当作 WS 帧解析，可能与微软侧限流有关。未深入，等用户指示。

## WS_PROTOCOL 根因全面分析（09:00-09:30，用户要求先分析 new-api 再下结论）

### 根因（证据链闭环）
- gorilla v1.5.3 conn.go：`for c.readErr == nil` —— 任何读错误永久写入 readErr，后续所有读直接失败；并发读共享 bufio 与帧解析状态。
- 时序证据：hit age_ms=1316 → 1s 后 WS_PROTOCOL；0x4C('L')=FIN0/RSV2_1/opcode12，即 JSON 中间字节被当帧头。
- 违约点唯一：returnConn=true 仅 1403 行成功路径；fresh 连接由 Chat 私有 reader 读，Return→startPark 起第二个 reader，旧 reader 阻塞在 ReadMessage（Return 清 deadline 后永久阻塞+泄漏）。
- 量化：24h 1752 hit / 2156 warmed / 337 parked；WS_PROTOCOL 220 ≈ 命中即坏 12.6%。坏 conn 只咬一人（失败→Discard），但每个成功请求都埋新雷。
- awaitReaderExit 半方案证伪（deadline 唤醒=毒化；不唤醒=永久阻塞，两难）→ 已 revert。

### new-api 借鉴（/tmp/new-api 浅克隆）
- 无连接池：每请求独立 HTTP，共享连接态 bug 类不存在。
- shouldRetry：错误分类驱动（channel error 必重试/skip-retry 不重试/状态码策略）。
- processChannelError→ShouldDisableChannel（关键词/状态码）自动禁用+channel-test 恢复。
- 核心启示：连接级错误≠账号级错误。当前 WS_PROTOCOL 触发账号 failover，破坏 pinned 会话连续性。

### 修复方案（待用户确认实施）
- B 根治：fresh 连接握手后立即 startPark，Chat 一律走 channels 消费（删 fresh 读分支），Return 重插 pc 不起新 pump。
- C 兜底（学 new-api）：WS_PROTOCOL/TCP 连接错误 → 同账号换新连接重试 1 次，不计账号健康、不触发 failover；再失败才走现有 failover。

## new-api 工具调用逻辑对比 + "两个一样的子任务"根因分析（10:15-10:45）

### new-api 的工具调用（/tmp/new-api 实证）
- **完全透传，零干预**：OpenAI 渠道 `ConvertOpenAIRequest` 不碰 `tools`/`tool_choice`（grep 无命中）；Claude 渠道 adaptor 同样不碰。工具只是 DTO 结构原样转发。
- 工具相关逻辑**只有计费**：`relay/channel/openai/helper.go` 流式里 `toolCount = len(choice.Delta.ToolCalls)`，最终 `usage.CompletionTokens += toolCount * 7`；`relay/common/tool_usage.go` 的 `ImageGenerationCallCounter` 用 id/call_id/index/result-hash 做**流式身份去重**（仅用于 image 计费）。
- **没有**"工具路由"层（不会额外发一轮 LLM 让模型决定调哪个工具），也不自建 MCP 暴露给上游。

### 我们的架构差异（这是温床）
1. 有 `model_tool_router`：额外一轮 LLM 决定工具，结果直接 `writeToolResponse` 返回（server.go:2101-2112）。
2. **工具双通道暴露（实锤）**：
   - 通道A：`clientPlugins()` 把客户端每个工具注册为 `Source:"API"` 插件（tools=16 → 16 个插件）。
   - 通道B：同时注册 `mcp-gateway` 插件，URL 由 `server.go:2013` 从**请求 Host 动态生成**（`%s://%s/v1/mcp/sse`），即指向本项目自己；而 `buildAnswerRequest` 又把**同一批工具** `MergeTools` 进 `GlobalToolRegistry`，MCP 的 `tools/list` 会返回它们。
   - → 上游模型看到两套同名工具。
3. **MCP 通道是僵尸**：`SetSessionTools` 从未被调用 → session.provider 恒为 nil → `tools/list` 能列出工具，但 `tools/call` 必然返回 `-32603 no tools available`。模型走这条必然失败，只能改走 API 插件通道重试。
4. **`GlobalToolRegistry` 只增不减**：`MergeTools` 每请求都调用，`ClearTools` 从未被调用 → 跨会话/跨用户累积历史工具，重名更多。
5. **router 成功路径无日志**（观测盲点）：只有 `len(calls)==0` 才打 `[router-nocalls]`，所以"48h 内 tool_calls_detected=0"是假象——工具调用全从这条静默路径出去了。
6. `parseModelToolDecision` **无去重**：模型输出 `{"calls":[X, X]}` 就产生两个 call（ID 用 index 区分，不会冲突）。唯一去重 `filterCompletedCalls` 依赖内存 ledger。

### 服务器观测（PID 253798，35d4f0c）
- 近6h 193 请求；router_start 107 / answer_start 仅 9 → 绝大多数请求在 router 轮就返回工具调用。
- 每请求都打 `[mcp] tools=16 mcp_gateway=http://microsoft.yorushika.cyou/v1/mcp/sse`；`/v1/mcp/sse` 被上游访问过 1 次。
- requestID 无重复 → 不是客户端重发。
- message 数每轮 +2（41→43→46→48→50），是正常 agent loop。
- **SSH 间歇性不可用**：`kex_exchange_identification: Connection closed`（服务端限流/临时封禁），退避重试第 15 次恢复（约 7 分钟）。期间 4141 也返回 empty reply。

### 待用户确认的修复方向
- 消除双通道（MCP gateway 指向自己时不再重复注册 API 插件，或反之）。
- 修僵尸 MCP：实现 provider 或不再暴露。
- router 成功路径补日志（count + names）。
- `parseModelToolDecision` 对相同 name+arguments 去重。
- 给 `GlobalToolRegistry` 加生命周期/清理。

## 五项修复全部落地（11:00-11:30，用户确认"按1-5做"）

提交 **63304f6**（9 文件，+191/-91），全量测试通过，已推 GitHub + 部署服务器。

1. **消除双通道**：删除 `chathub.Request.MCPServerURL` 字段、`clientPlugins`/`callerCanExecute` 的 mcpServerURL 参数及 mcp-gateway 插件注册；`buildAnswerRequest` 去掉 mcpServerURL 参数与 MCP 转发块。工具只走 API 插件单通道。`server.go` 原 `[mcp] tools=N mcp_gateway=...` 日志改为 `[tools] id=N declared=N transport=api-plugins`。
2. **去重**：`parseModelToolDecision` JSON envelope 分支按 `name+"\x00"+canonical args JSON` 去重，ID 用 `len(out)` 保持连续唯一。新增回归测试 `TestParseModelToolDecisionDeduplicatesIdenticalCalls`。
3. **修僵尸 MCP**：`handleRPC` 的 `tools/list` 对 nil-provider session 返回空列表（不再回落 GlobalToolRegistry 广播不可执行工具）；`tools/call` 错误信息改为明确"MCP transport is discovery-only"。`/v1/mcp/tools` 保留为纯发现端点。
4. **注册表生命周期**：新增 `ReplaceTools`（快照替换），`buildAnswerRequest` 改用它；`MergeTools` 标记 Deprecated 保留。新增 `internal/mcp/registry_test.go`（镜像最新请求 + 并发冒烟）。
5. **router 成功路径日志**：新增 `logRouterToolDecision` helper，三处 `writeToolResponse` 成功出口（stream-router / router / router-required-retry）统一打 `[tool-router] id= account= stage= decision=call count= names= elapsed_ms=`。

部署流程（沿用 2026-09-08 记录，全部顺利）：
- 本地 `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -trimpath -ldflags "-s -w -X ...web.Version=v0.6.6 -X ...Commit=63304f6 ..." ./cmd/server` → 21.5MB。
- GitHub 直推被代理静默吞掉（exit 0 无输出、origin/main 不动）→ 改服务器中转：bundle scp 上去，服务器脚本 clone --bare + fetch bundle + push，**PUSH_OK**。脚本含 token，服务器端 `shred -u` 自毁 + 本地副本已删。
- 部署：备份 `.bak-pre-63304f6` → install 755 m365:m365 → restart → 新 PID **255800**，active，listening 4141。
- 验证：`journalctl` 近 5 分钟 `mcp_gateway|v1/mcp/sse` 命中 **0**（新日志不再出现双通道痕迹）；/v1/models 401（鉴权正常）。
- 坑复发：本地 `origin/main` loose ref 又消失，`mkdir -p .git/refs/remotes/origin` 后直写修复。**验证远端状态一律 `git ls-remote`/fetch 输出，不信本地 ref 文件**。

遗留观察项：等真实 Codex 流量进来后看 ①是否再出现"两个一样的子任务"②`[tool-router] decision=call` 日志是否正常输出③GlobalToolRegistry 是否只含最新请求的工具。

## 沙箱幻觉复发 → 定位为 63304f6 回归并修复（11:55-12:10）

- 用户报告："目前无法继续实际修改：本会话的执行环境中没有 D:/M365-Copilot2API，/mnt/data 也是空目录"。
- 日志定位：全部发生在 **router NO_TOOL_NEEDED → answer 轮**（`answer_start native_tools=0`）。
- 根因：63304f6 删除 MCP 网关时，连带删掉了 answer 轮 `CanExecute=true` 的唯一信号源。router 模式 answer 轮 req.Tools 恒为空 → `callerCanExecute(nil)=false` → toolProtocolPrompt 不再注入 Windows 执行守卫 → 模型回到 Linux 沙箱幻觉。**守卫此前是被双通道"顺带"保住的，删通道时必须显式补回信号。**
- 修复（02214e4）：`chathub.Request` 新增 `CallerCanExecute` 字段，`buildAnswerRequest` 按 `len(body.Tools)>0` 设置，chatPayload 里 `CanExecute: req.CallerCanExecute || callerCanExecute(req.Tools)`。无工具纯聊天仍不加守卫。含回归测试 `TestBuildAnswerRequestCallerCanExecuteSurvivesRouterMode`。
- 已推 GitHub（服务器中转，直推再次被静默吞掉）+ 部署 PID **257308**。
- 坑：修本地 origin/main ref 时手抄完整哈希抄错尾段（02214e4cf10d**45c132**…），导致 "nonexistent object" 假象，差点又误判为 ref 神秘回退。**教训：写 ref 一律用 `git rev-parse main` 的输出，不要手抄。**

## 部署 02214e4 后 2.5h 监控（14:35，PID 257308）
- 112 请求：110×200 + 20×401（管理端鉴权探测）+ **0×429/5xx**；router 决策 70 次**全部 count=1**；WS_PROTOCOL=0；mcp_gateway=0；router 失败=0；panic=0。
- **幻觉话术 0 命中**（mnt/data、无法修改等关键词）→ 守卫信号修复生效。
- 工具分布：Read×30 Bash×27 Agent×5 TaskOutput×3 SendMessage×3 Skill×2。
- 1 条轻微异常（04:13）：router 原始输出 `CALL_TOOL: Read({"file_path":...,"offset":1783,"limit":90})`，repair 后仍 0 调用（parsed=true）——router 模型编了 Codex Read schema 不存在的 offset/limit 参数被 schemaValid 拒掉，浪费一轮 repair 后走 answer。1/70 频率，观察即可。
- 之前 grep 命中的"26 条 429"全是 UUID 十六进制巧合（429b/4298），查日志数字模式要精确锚定 `status=`。

## 日志驱动优化三项落地（14:45-15:05，参考 new-api 最小干预哲学，0b190cd）

从 2.5h 监控数据发现的三个性能病灶（router 轮 avg 11s / p90 16.4s，prompt avg 128KB / max 585KB）：

1. **Ledger 证据渲染无上限（主因）**：RouterContext 全量序列化所有 completed 工具证据（每条 result 上限 4000 字符），30 次调用 ≈ 120KB，slim 后的 77KB prompt 大部分是它。改为只渲染最近 10 条（result 截 300 字符），更早的折叠成计数；hasCompleted 去重仍用全量 ledger，语义不变。
2. **指代词升级过宽**：扫描窗口内所有 user 消息，"继续/continue/above" 等高频词导致 54/75(72%) 请求升级全量历史，瘦身失效。改为只扫最新一条 user 消息（指代本质是当前轮属性）。
3. **conv-cache 在 router 工具请求下是死重**：router 轮会话用完即弃，缓存永不命中（2.5h 内 93 查 0 中），每请求白算 systemPromptHash。跳过（routedToolTurn 判定），miss 日志也不再刷屏。

部署：PID **259586**，GitHub=本地=0b190cd（直推失败一次后走中转）。备份 .bak-pre-0b190cd。
预期：router prompt 显著变小（ledger 占比 ~60%→<15%），上游处理时间下降，p50 延迟可观测改善；等下一波流量对比 elapsed_ms。

## 监控 + "一上来就开子 agent" 根因修复（15:05-15:15，8a514bf）

- 监控（部署前旧流量）：89 请求 71 决策全 count=1；但 p50 prompt_len 仍 69KB（旧二进制数据，0b190cd 效果待新流量）。
- **用户问题："为什么每次一上来就开子 agent"**——日志证实：新会话首个工具决策就是 `Agent`。
- 根因（结构性偏置）：router 每轮只选**一个**下一步，且提示词只教单条 `CALL_TOOL` 格式。"一次调用干最多活"的选择永远是 subagent 启动器，多步任务被理性地打包进 Agent。new-api 对照：原生 function calling 一轮可发多个 tool_calls，我们文本协议没给这个表达力 → 全塌缩成委派。
- 修复（8a514bf）：`hasDelegationTool` 检测委派形工具（agent/task/*subagent*/*delegate*/*spawn*），命中时 router 提示词追加两条规则：① 直接工作自己做，委派工具仅在用户明确要求或直连工具做不到时用；② 独立多步用 JSON 并行 envelope（`{"calls":[...]}`）而非打包进 subagent。含测试。
- 部署：PID **260094**，GitHub=本地=8a514bf（中转 PUSH_OK），备份 .bak-pre-8a514bf。
- 待观察：新会话首个决策是否不再清一色 Agent；names 分布中 Agent 占比；count>1 的并行决策是否出现。

## 委派判断修正 + 上下文窗口根因（15:35-15:50，1c61839）

1. **委派规则修正**：8a514bf 的规则"用户明确要求才委派"不符合实际（用户从不明确要求）。参考 new-api 透传哲学：委派与否应由 AI 对照**工具自身 description** 判断（大型、自包含子任务才委派），日常步骤用直连工具。规则已改写。

2. **Codex 上下文窗口根因（读 codex-rs 源码确认）**：
   - 触发条件：`auto_compact_token_limit = 90% × context_window`；**context_window 为 None 且未配置 `model_auto_compact_token_limit` → 返回 None → 永不触发**（回退 i64::MAX）。
   - `gpt-5.6-sol` 这类自定义模型名匹配不到已知 family → context_window=None → **永不压缩**，即用户症状。
   - 网关侧已修：`writeResponsesResult` 兜底估算原来传 nil（input 恒 ~7），Codex 靠 usage 块驱动上下文计量表，常数计量=永不触发。现在传真实 o.Messages/Tools/ToolChoice（含回归测试）。流式适配器本来就用真实请求，无需改。
   - **客户端侧修复（用户需在 Codex config.toml 加）**：`model_auto_compact_token_limit = 200000`（此配置独立于 context_window，设了就生效）。注意 `model_context_window` 在 0.116/0.117 会静默失效甚至破坏压缩，别用。
   - 另：`wire_api="chat"` 2026-02 已从 Codex 移除（硬报错），只能走 Responses——网关 /v1/responses 支持正确且必要。
   - 今日流量全是 /v1/chat/completions（ZCode），input_tokens 估算真实（97k~238k）。

部署：PID **261220**，GitHub=本地=1c61839（中转 PUSH_OK），备份 .bak-pre-1c61839。

## /v1/responses 秒失败修复（15:55-16:05，24eaec8）

- 用户报：/v1/responses 秒返回 `reason=empty_model_response`（客户端话术）。
- 日志定位：`[responses] inner-reject status=400 code=tool_protocol_error detail="tool results missing before assistant message at index 197"`——长会话历史里 tool_call/result 配对断裂，旧 validateToolConversation 直接 400 杀掉整个请求。
- 修复：改为 `repairToolConversation` 自愈——缺结果→插入占位 tool 消息；孤儿结果（call 被客户端压缩掉）→丢弃；重复 call id→去重；结尾未闭合→补占位闭合；仅 call 无 id 仍报错。日志 `[tool-state] repair=...`。
- 顺手：`reasoning`/`item_reference` 输入项不再掉进 default 分支变 user 消息。
- 坑：`out := messages[:0]` 边遍历边写覆盖未读元素（别名 bug），必须新开底层数组。
- 部署 PID **261534**，GitHub=本地=24eaec8，备份 .bak-pre-24eaec8。等用户重试 /v1/responses 验证。

## /v1/responses invalid_request 根因（16:05-16:25，d7e3b1e）

- 用户报：Turn execution failed reason=invalid_request retryable=false（ZCode，session eaf69f91，turn 29）。
- 网关侧 08:04:38 请求 d0c50d2e 全程正常（repair 自愈触发、router 决策 Agent、200 返回 9937 字节 SSE），客户端无第二次请求 → 失败在客户端解析我们 200 流的过程中。
- **ZCode 运行时日志（~/.zcode/cli/log/zcode-2026-09-09.jsonl）给出确切错误**：`AiSdkModelAdapterError: Model returned an invalid tool call: tool name is empty.`（normalizeModelToolName，streamText）→ 包装成 reason=invalid_request。
- 根因：streamResponsesAdapter 在创建 tcState 时**先 emit `response.output_item.added`（name=""/call_id=""）再解析声明 chunk**。ZCode 的 AI-SDK 适配器在 added 事件上就校验工具名，空名直接杀整轮。ZCode session/resume 显示 apiFormat=openai-responses、baseURL=microsoft.yorushika.cyou/v1。
- 修复（d7e3b1e）：added 事件推迟到拿到工具名才发（声明 chunk 恒先于参数 delta，顺序安全）、带真实 call_id/name；畸形无名调用丢弃并打日志；新增 `runResponsesInnerChat` 接缝 + 回归测试（驱动适配器回放 writeToolResponse 形状的流）。
- 排查技巧沉淀：ZCode 主日志（v2/logs）没有 agent 细节，**agent 运行时日志在 `~/.zcode/cli/log/zcode-*.jsonl`**，请求/响应原文在 `~/.zcode/cli/rollout/model-io-sess_*.jsonl`；`[req-trace] http_start stream=%t` 读的是 URL query 不是 body，别被误导。
- 部署 PID **262067**，GitHub=本地=d7e3b1e（PUSH_OK），备份 .bak-pre-d7e3b1e。
