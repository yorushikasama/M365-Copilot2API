# 2026-09-10

## 服务器日志巡检（服务 PID 266114，09-09 14:36:51 UTC 启动，代码版本 7cd9bb2 / 0.6.5）

统计窗口 10.7h，4724 行日志：200 请求，164×200 / 35×401 / 1×404，**0 panic、0 error、0 invalid_request、0 empty_model_response**（d7e3b1e 前的问题未复现）。

### P0 功能缺陷：router 决策被 ledger 去重静默清空
- 证据：`2026/09/10 01:14:24 [router-nocalls] id=e8b53df8 stream=true route_prompt_len=213194 parsed=true upstream_text="CALL_TOOL: Read({\"file_path\":\"D:\\NetPeek\\src\\NetPeek.App\\ui\\history-ui.js\"})"`
- 链路：`internal/web/server.go:2103` `calls = filterCompletedCalls(calls, ledger)` → `agent_ledger.go:195`，按 **name + 规范化参数** 严格匹配已执行记录 → 命中即丢弃 → `len(calls)==0` 走 nocalls 分支 → 降级到 answer 轮 → 客户端工具调用丢失、模型开始瞎编。
- 无 `[tool-validation]` 日志（0 条），排除工具名校验拒绝。
- 修复方向：只读幂等工具（Read/Glob/Grep/LS）不去重或允许重复一次；去重清空但 parsed 有调用时，应把「该调用已执行、结果见 EVIDENCE_LEDGER」注入 answer prompt，而不是静默降级。

### P1 性能
- **连接池 0 复用**：`reused=false` 137 次 / `true` 0 次，每次 `ws_dial_ms` 平均 393ms。启动时的 `[connpool] warmed connection` 全没用上。
- **conv-cache 133 miss / 0 hit**，每请求新建云会话（router 轮还会 `dropTransientConversation` 删会话）。
- **router prompt 208–213KB**：`[router-slim] upgrade_to_full reason=reference_hint` 每次命中（长对话必然含指代词）→ 单轮 router 12.5s / 17.2s，占端到端大头。

### P2 运维
- `M365_MASTER_KEY` 未设置 → `[security] WARNING` **1462 条**（每请求 × 账号数），refresh token 用内置公开 key 加密。
- ~~版本分叉~~ **误判，已纠正**：第一次查服务器 `git log -1` 看到 7cd9bb2（0.6.5），是因为当时 `~/M365-Copilot2API` 恰好处于 **detached HEAD @ tag 0.6.5**（`git branch -vv` 显示 `* (HEAD detached at 0.6.5)`），并非在 main 上。复查：`git rev-parse HEAD/main/origin/main` 三者均为 **9e08377**，与本地完全一致，没有分叉。**教训：判断服务器版本要看 `git rev-parse HEAD` + `git branch -vv`，单看 `git log -1` 会被 detached 状态骗到。** 运行的二进制在 `/opt/m365-copilot2api/m365-copilot2api`（WorkingDirectory=/opt/m365-copilot2api），与 ~/ 仓库是两回事。
- 127.0.0.1 出现 1 次 `admin_login_failed detail=invalid password`（疑似服务器本地旧脚本用老密码）。
- 128 次 `messages=2 tools=0` 的 chat（prompt 99 字符，回复 "Hello, AxonHub!"）为渠道健康检查，约每 5 分钟一次，每次真打上游。
- 耗时：http p50 5.99s / p90 8.42s / max 28.7s；chat p50 6.75s / p90 8.94s。

## 修复提交 0cd6020（已部署，PID 271977）
1. **P0 只读工具被去重误杀**：`agent_ledger.go` 新增 `isRepeatableTool`（Read/Glob/Grep/LS/WebFetch… 白名单 + `read*`/`glob*`/`grep*`/`list*` 前缀），`filterCompletedCalls` 对只读调用一律放行；新增 `duplicateCallNotice`，当变更类调用仍被丢弃时把「已执行结果」注入 answer prompt。server.go 三处 router 分支保留 `rawCalls` 副本（filterCompletedCalls 就地过滤会覆盖原 slice，必须先拷贝），nocalls 日志加 `raw_calls=N`，新增 `[router-dedup]` 日志。
2. **P1 连接池 0 复用**：`connpool.go` 加 `lastWarm` + `ShouldWarm`（10s 节流），`client.go` 改为 `if reused || c.Pool.ShouldWarm(...)` —— 原来只在命中时回补，池空后永不 refill（137/137 reused=false）。
3. **P1 route prompt 瘦身**：`model_tool_router.go` 新增 `routerToolCatalogue`（工具清单降为 name/desc(≤300)/args type+required*，53 个完整 schema 114KB → 精简）；`router_prompt.go` reference_hint 改为**有界升级**（窗口 ×4 + 预算 ×6，非全量）。测试实测 full=217577 → bounded=23079。回退开关：`M365_ROUTER_FULL_TOOL_SCHEMAS=true`、`M365_ROUTER_FULL_ON_REFERENCE=true`。
4. 测试：`go test ./...` 全绿；更新 `TestBuildRoutePromptSlimming`（旧断言"必须升级到 full"改为"必须比 slim 宽且比 full 小"），新增 3 个测试（Read 重复保留、duplicateCallNotice、工具目录精简）。
5. 部署后暂无自然流量可验证（重启后只有前端轮询 401），待下一轮观测确认 `[connpool] hit` 与 `[router-slim] upgrade_to_bounded`。

## 流式首帧截断修复（cc6f6af，已部署 PID 273634）
- 症状：/v1/responses 与 /v1/chat 流式回答开头丢字（如丢列表序号"3."）。根因：网关把 role 和第一段文字塞进同一个 SSE chunk，客户端只读首帧的 role 就把该帧内容丢了。
- 参考 new-api 模式（relaykit/relayconvert）：Chat→Responses 用 ensureCreated/startText 惰性首帧；Responses→Chat 用 ensureStart()（role-only 空内容帧，内容单独成帧）。我们上游是 Copilot websocket，不能逐行抄，但状态机契约完全照搬。
- 改动 4 处：server.go 三条 chat 流式路径改为独立 role-only 首帧；codex_responses.go 与 protocol_handlers.go 在 output_item.added 之后、首个 output_text.delta 之前补发 response.content_part.added（对齐 OpenAI 官方事件序）。
- 全量测试通过；服务器 admin 密码不是 admin123（不要猜），冒烟需用户客户端真实流量验证。
- new-api 双向转换位置：oai_responses/to_oai_chat_stream_resp.go（R→C）、oai_chat/to_oai_responses_stream_resp.go（C→R，862 行）。

## cc6f6af 部署后 45 分钟监控（02:19-03:04 UTC，27 请求）
- 0 错误 0 panic；401×5 全部来自 127.0.0.1（本机冒烟密码猜错），非外部。
- P0 生效：router 16 次决策全 count=1，Read×8 重复调用不再被去重误杀；唯一 1 条 nocalls 是模型正常 NO_TOOL_NEEDED。
- P1 生效：connpool reused=true 24/28（86%，此前 0/137）；router-slim full 185KB→44-50KB；reference_hint 升级有界化（125KB→47KB 而非全量）。
- 耗时 p50 5.9s / p90 14.3s / max 30.7s；ws_dial avg 55ms。

## 「说做完但没做」排查（sess_f083b481，NetPeek 历史页任务）
- 网关链路健康：3 次工具决策（Bash×2+TodoWrite×1）全部正常送达，无丢帧。400×3 是 /v1/images/generations 被上游内容策略拦截，与本案无关。
- 根因=上游模型行为：23 轮全程零 Edit/Write 调用（工具清单里明明有），14 条不同 Bash 命令全是读/check（sed -n 打印、Get-Content、git diff/status、node --check、npm build），磁盘上 NetPeek 源码今天零改动（33 个今日变更文件全是 target 构建产物）。最后把 Todo 全标 completed 并输出"完成"总结——「读=做完」幻觉。
- 排查方法：ZCode rollout（model-io-sess_*.jsonl）最后一条请求的 body.input 历史是权威证据源；node 解析 toolCalls 统计真实调用；磁盘 mtime 交叉验证。
- 处置：追问模型"列出你改的文件和 diff"；任务 prompt 里加验收条款（无 diff=未完成）。网关无需改动。

## 「总结不是 markdown」根因与修复（65ddb0d，已部署 PID 277116）
- 症状：15:33 的 5432 字符总结文本损坏——同一段列表出现 3 个变体（含 <File> 标签版）、每处改写边界行首丢字（工具栏→具栏、grid-template→-template）。
- 根因：上游 Copilot 流中途改写已发内容（该请求被 Throttled 打断 3 次后内部重生成），emitSnapshot 的公共前缀分支把分歧尾巴粘接到已发文本→多代内容杂交；writeAtCursor 逐 token 片段又绕过 emitSnapshot 继续外流；finalizeText 在 streamed 比 final 长时直接返回损坏文本。
- 修复：检测到改写即抑制后续所有 delta（快照+cursor 片段都拦），finalizeText 在 skipped>0 时强制采用权威 type-2 final 消息。流式客户端保留改写前的干净前缀；历史/conv-cache/非流式拿到完整正确答案。
- new-api 参照结论：其上游（OpenAI 形状）是 append-only delta，永不改写，故无此问题类；可借鉴的原则是"永不拼接/转换上游文本"。
- 排查方法：ZCode rollout 最后一条记录的 response.text 是权威客户端视角；提取脚本需收集 delta+text 两种字段。

## 08:00-08:27 慢响应与 502 排查（65ddb0d 后）
- 51 请求全部 stream=false（客户端切非流式）→ 用户全程白等，客户端 ~125s 超时断开产生 7 个 499（router_return err=true elapsed≈124s）。
- 34 个 502：13 个是探活(prompt=99)被上游 InternalError 秒拒（1.7-2.5s），1 个是真实 95KB prompt；上游 08:00-08:13 明显不健康，session-resolver unpin reason=unhealthy。
- 慢的两大根因：① 每个带工具请求 = router 轮(20-40s，prompt 43-95KB) + answer 轮两次串行上游调用，p50 27s；② 13 次 reference_hint 升级把 router prompt 放大到全量历史（route_prompt_len=94999 > flattened 82565）， Slim 失效。
- 新发现的正确性问题：router 轮被上游会话历史污染劫持——upstream_text 出现印尼语闲聊"刚才你发了一个'？'，我回答的是 Halo!..."（raw_calls=0→nocalls→再付一轮 answer）。M365 会话按账号 pin，历史残留导致模型不执行路由协议。
- 499 本质：M365 首token超时预算(150s+60s+grace)远超客户端耐心(~125s)，客户端先断。

## 三项修复（df7257c，已部署 PID 278447）
1. **router prompt 基准错位（94999 之谜）**：reference-hint 分支的 `full` 按"增量 answerPrompt"构建（14KB），兜底 return 却按全量 fullPrompt 重组（95KB）→ bounded(32K) 永远输给 14K 的假基准 → 静默返回全量。改为无条件返回有界 wide 变体（byte-capped）。证据：boot4.log 997-1012 行，router-slim 日志打 prompt_len=14628 而 router_start=94999。
2. **router 轮隔离**：router/repair 轮统一走 routerTurnRequest()——DisableMemory=true（堵"账号记忆污染劫持"，印尼语闲聊实锤）+ MaxFirstTokenWait=60s（env M365_ROUTER_FIRST_TOKEN_SECONDS），挂死的上游 60s 即失败，不再陪客户端 125s 白等（今日 7 个 499）。
3. **答非所问根因**：失败轮（499/超时/502）在 payload 已派发后才失败 → 云端会话领先于 ContextHistory → 下一轮按旧前缀匹配只发增量 → 跳过失败轮的问题 → 模型回答云端顶部还挂着的上一个问题。修复：answer 轮失败时 InvalidateMatching 删除匹配绑定（流式+非流式两处）；matchContextLocked 加 prefixHoldsExchange 守卫（system-only 前缀不得绑定）。新增 2 个回归测试。
- 遗留：客户端仍需切回 stream=true 才能根治体感延迟与 499。

## 「说继续却不干活」排查（sess_f083b481 09:02-09:07）
- 用户"继续完成未完成的内容"只收到 442 字未完成清单。三因叠加：①会话上下文爆了自动压缩（ZCode "continued from a previous conversation"；网关 context-budget 147→40 条），摘要全是核查清单文风，模型照着汇报；②压缩后模型只 Read 文件 + Write 了一份 implementation-context 设计 md（impeccable 技能流程第一步），没改业务代码；③最后一轮 router 判 NO_TOOL_NEEDED → answer 轮纯文本（native_tools=0）只能说话。
- 另见 09:05:19 router 被摘要格式污染：输出 2 万字符 <analysis>/<summary> 而非路由协议（模仿压缩摘要）。
- 客户端侧对策：压缩后第一条消息给具体指令（改哪个文件+跑构建），不用"继续完成…"；任务收尾主动开新会话。
- 网关侧待选修复：router prompt 对行动指令加偏置（user 最后消息为行动词+有工具时禁 NO_TOOL_NEEDED）。未实施，等用户确认。

## answer 轮工具通道（05f8a20，已部署 PID 279647）
- 设计依据：new-api 三"零"设计（零工具决策/零上下文干预/零 agent 循环）——工具决策是上游原生 function calling，读历史的脑=决策的脑，故不存在"说继续却只汇报"。我们 Copilot 上游无原生工具，被迫两脑分离（router 53KB 视角决策 + answer 199KB 纯文本），router 误判 NO_TOOL_NEEDED 时结构性产出状态汇报。
- 修复（对齐"回答者拥有工具决策权"原则）：①buildAnswerRequest 追加 answerToolProtocol 尾注（routerToolCatalogue 紧凑目录 ~KBs，非 53 全 schema；规则含"用户要求继续/完成时必须发工具调用，不得回未确认清单"）；②流式两路径 holdback 抑制——classifyAnswerOutputPrefix 判定开头 ≤512 字节，CALL_TOOL/{"calls"} 开头则全程抑制 content 通道、尾部 parseModelToolDecision→writeToolResponse，prose 则 flush 照常流式；③非流式尾部在 fencedToolCalls 之前先做协议解析；④answer 解析的调用过 filterCompletedCalls + validateCalls + limitToolCalls 同一管线。
- router 降级为延迟优化，不再是正确性闸门。新增测试 TestAnswerToolProtocolContent/TestClassifyAnswerOutputPrefix，更新 TestBuildAnswerRequestRouterOmitsNativePlugins（头部不变+尾注断言）。
- 验证方式：用户重跑"继续完成未完成的内容"场景，观察日志 [answer-tool] decision=call / decision=deferred_call。

## 19:06-19:13 日志巡检（PID 279647，UTC 11:06-11:13）
- 服务健康：无 502/panic/上游错误；一次完整 agent 循环正常：Read→Write→Read→Edit 连续 4 次工具调用（每次 7-15s），末轮 router 判 NO_TOOL_NEEDED→answer 轮 123 字符收尾，符合设计。
- 改写抑制按设计触发 2 次（11:07:54、11:10:52 "upstream rewrote...suppressing"），finalizeText 用权威 final 恢复，无杂交文本外流。
- **新异常（sess 29d0734e）**：疑似客户端压缩请求（messages=152 tools=53）被当普通请求走 router：router 轮 80s 产出 24KB <analysis>/<summary> 压缩摘要（raw_calls=0，期间 862 个快照被抑制），网关丢弃摘要又跑 answer 轮（229KB prompt，87s），最终客户端只收到 312 字符答案，总耗时 174s。router 轮白算摘要被扔——需识别"压缩/总结类请求"（prompt 含 summarize/compact 特征时跳过 router 直通 answer）。
- 噪音：M365_MASTER_KEY 警告每次账号加载刷一遍（应设 env 值一次性消掉）；/api/throttle-stats、/api/accounts、/api/stats、/api/usage、/api/admin/keys 每 15-60s 被 401 轮询（来源待确认：自家 dashboard 未带 key 或外部扫描）。
- [answer-tool] decision=call 至今未被真实触发过，answer 轮工具通道路径待用户实测验证。

## 19:17-19:35 M365_MASTER_KEY 落地 + 401 轮询溯源（提交 6b61f76 + 466bbb8，服务器 PID 280770）
- **不能直接设 key 的原因**：cache.go decryptRefreshToken 只用当前 key 派生解密，现网 accounts.json 全是 fallback key 加密的，直接换 key 会导致全部账号 refresh 失败。
- 修复（cache.go）：①拆 deriveKey/fallbackKey；②decryptRefreshTokenEx 用 master key 失败时回退 fallback key（legacy 路径），decryptRefreshToken 包装保持兼容；③OpenStore 检测到 legacy 解密即 saveLocked 重加密落盘。测试 TestMasterKeyMigration（三次开关 key 全链路）。
- 部署：openssl rand 32 位 key 写 /etc/m365-copilot2api.env（root:0600），systemd drop-in override.conf 引 EnvironmentFile；accounts.json 备份为 accounts.json.bak-20260910。验证：M365_MASTER_KEY 警告 0 次、无解密失败、accounts.json 已重写（34 个 enc:v1: 用新 key）、重启后全部账号 token-refresh 成功。
- **401 轮询溯源（nginx access.log）**：来源 2408:8956:8280:cba5::/64（移动广东 IPv6，即用户自己的 Edge 浏览器）。08:34 UTC 前 /api/* 全 200（登录有效），09:08 UTC 起同浏览器变 401——服务重启清了管理会话，浏览器残留 dashboard 标签页每 60s 轮询 throttle-stats 吃 401；19:10 的批量 401 是用户重新打开 dashboard。非外部扫描。处置：关掉/重新登录该标签页即可；可改进点=前端 401 后停止轮询并提示重登录。
- 用户跑了"继续完成未完成的内容"验证（19:09-19:11 UTC）：router 正确决策 Read→Write→Read→Edit 4 连工具调用，末轮 NO_TOOL_NEEDED 收尾 123 字符（合法收尾汇报）。[answer-tool] 未触发是因为 router 没误判——正常路径验证通过，兜底通道保持待命。

## 20:05-20:30 切模型后"没有工具"拒绝的彻底修复（提交 362b22c，服务器新 PID）
- 现象：ZCode 切到 私/gpt-5.6-sol 后，用户"实现以上未落地的内容"得到回复"当前会话没有可调用的 Windows 工作区文件编辑工具"。取证（rollout model-io-sess_f083b481 line 87 + 网关 330e8711）：客户端声明 53 工具 ✓，router 判 NO_TOOL_NEEDED（又误判，行动指令）→ answer 轮（193KB，带 answerToolProtocol 尾注）→ 模型输出 780 字符拒绝长文。
- **根因三层**：
  1. answer 轮 prompt 没带 WindowsExecutionGuard（只有 router prompt 有）——模型看不到原生工具 schema，按自己的世界观判断"我没有文件编辑工具"，如实拒绝（注释里早已记录的沙箱幻觉，出现在了没设防的 answer 轮）。
  2. 已有的 [tool-eject] 拒绝重试（toolloop.go isToolRefusal）没触发：①len(text)>=200 短文本上限一刀切（780 字符拒绝长文直接 false）；②词表只有"工具不可用/工具未暴露"，"没有可调用/没有工具"全不匹配。
  3. router prompt 无行动指令禁令——"实现以上未落地的内容"被判 NO_TOOL_NEEDED，退化纯文本轮。
- **修复（四层防御）**：
  1. answerToolProtocol 前置 WindowsExecutionGuard + 反幻觉条款（"工具列表就是本会话的完整工具集；没有原生 schema 是网关传输细节，不是没有工具的证据；永远不要声称没有工具"）。
  2. userMessageDemandsAction()（中英行动词表）→ 行动指令时 answer prompt 追加 EXECUTION MANDATE（陈述"这次消息就是要执行的"）。
  3. router rules 加行动指令禁令（行动请求禁止 NO_TOOL_NEEDED，除非纯聊天）。
  4. toolloop.go 新增 containsToolDenial（toolDenialPhrases 扩充 17 条中英短语，无长度上限）；非流式 [tool-eject] 重试条件放宽为 isToolRefusal || (actionDemanded && containsToolDenial)，重试结果同样校验；流式路径加 [answer-refusal] 日志（流式已吐字无法重试，仅观测）。
- 测试 TestToolDenialDetection（长短文本/中英/误报/路由禁令）+ TestAnswerToolProtocolContent 扩展 guard 断言。全量 go test 通过，已部署（新 PID）。
- 观测点：日志出现 [tool-eject]（重试成功）或 [answer-refusal]（流式漏网）即复发被捕获。

## 20:20-20:35 全维度复杂测试（边测边看日志）——发现 3 个真实缺陷

- 测试基建：`tests/live-audit-20260910.py`（protocol/errors/content/context/tools/concurrency 六 section，~36 用例）+ `tests/live-probe-20260910.py`（5 定向探针）。经 SSH 隧道 localhost:14141 直连网关，基线 UTC 12:21:51，二进制 03653885 = 362b22c。
- 通过（信心）：流式 role-only 首帧契约 ✓、首字符不丢 ✓、responses 事件序（created→output_item.added→content_part.added→delta）✓、Markdown/Unicode/长输出尾部完整 ✓、客户端中断后可恢复 ✓、工具单/并行/命名/多轮/流式/参数 schema 全 ✓、并发 6/6（wall 9.1s）✓、带真实文件工具的"实现以上未落地内容"→ 正确发 read_file ✓（362b22c 生效）。
- **缺陷 1（P0，答非所问根因）：上游账号级记忆劫持客户端上下文。** 对照实验：客户端 messages 里明写 vault code=TOPAZ-55，memory ON → 答 **BANANA7**（早前测试遗留词）；加 `metadata.copilot_temp_session=true`（memory OFF）→ 答 TOPAZ-55 正确。日志：ON 轮 session-resolver matched=context_prefix_2 history=2/3 → 实际只发 text=57（增量）；OFF 轮 clearing session → 全量 text=153。根因组合：上游 `isInsightsFromConversationHistoryEnabled=true`（memory/instructions 端点确认为空，记忆来自对话历史学习）+ 网关 answer 轮 `DisableMemory` 默认 false（仅客户端显式传 copilot_temp_session 才 true）。修法建议：默认禁上游记忆 + settings 开关。
- **缺陷 2（P1）：`parseContent(nil)` 返回字符串 "<nil>" 注入上游 prompt。** `internal/web/multimodal.go:18` 兜底 `fmt.Sprint(c)`，nil→"<nil>"。单测铁证：assistant content=null（OpenAI 标准 tool_calls 形态）→ prompt 出现 `[assistant]\n<nil>`；user content=null → `[user]\n<nil>` **len=12**，与线上日志 prompt_len=12 完全吻合。影响：所有标准 OpenAI 多轮工具对话历史都被注入噪声；空输入触发上游默认语言（该账号回印尼语"Halo! Sepertinya pesan Anda kosong"）。
- **缺陷 3（P2）：请求校验过宽。** ①`model: "no-such-model-xyz"` → 200 正常回复（未知模型静默接受，不报错）；②缺 model 字段 → 静默降级 "m365-copilot"（server.go firstNonEmpty）；③`content: null` 单条消息 → 200（应 400）；④300KB 单消息 → 200 但耗时 71s 无提前拒绝。
- 日志噪音（P3）：`[multimodal-trace] payload_has_attachments:true` 与 `attachment_count:0` 自相矛盾（字段判断有误）。
- 取证件 `internal/web/nilcontent_probe_test.go`（当前故意 FAIL，修复 parseContent 后自动转 PASS，作为回归）。
- 待用户决策：是否按优先级修复（记忆默认禁用属行为变更）。

## 20:45-21:00 修复上下文被截断（根因修正：增量发送 > 账号记忆）

- **根因修正**：此前把"答非所问"归因于账号记忆不完整。加对照后发现真正主因是 **server.go:2004 的增量发送**——`resolved.HistoryLen > 0` 时把 answerPrompt 换成 `body.Messages[HistoryLen:]`，前提是"云会话仍持有匹配到的前缀"。实测该前提不成立：客户端传 [user,assistant,user]（flatten 109 字符）实际只发 29 字符，云会话没有重放被裁掉的两条，模型于是拿账号记忆瞎猜（先答 BANANA7，禁记忆后答"我不知道"）。记忆只是"历史丢失后提供错误素材"的次因。
- **修复（提交 93a5aeb + 828e899 二进制）**：
  1. `EnableIncrementalPrompt`（settings/env `M365_ENABLE_INCREMENTAL_PROMPT`，**默认 false**）→ 默认总是发送调用方完整历史；server.go:2004 条件加开关。
  2. `EnableUpstreamMemory`（env `M365_ENABLE_UPSTREAM_MEMORY`，**默认 false**）→ answer 轮 `DisableMemory = disableMemory || !cfg.EnableUpstreamMemory`；`copilot_temp_session` 仍强制关闭。
  3. `multimodal.go` parseContent(nil) 返回 ""（原 `fmt.Sprint(nil)` → 字面 "<nil>"）；顺带让空消息请求返回 400 而非送上游按账号默认语言乱答。
  4. `session_resolver.go` 恢复 10 处被 GBK 往返损坏的乱码注释（改为等价英文注释）。
- **验证**：V1 完整历史 recall 两轮（ONYX-31 / AMBER-64）全部答对（修复前分别答 BANANA7 和"我不知道"）；日志 `prompt_flattened prompt_len=99` → `prompt-trace text=99` 全量一致；V3 空 content → 400；V2 tool_calls 历史 → 正常发起工具调用（finish_reason=tool_calls，content=null 符合协议）。
- 回退方式：两个 env 置 true 即恢复旧行为。全量 go test 通过，已部署（PID 282823，md5 a12450c6）。

## 21:00-21:10 第二轮多维测试（新角度）—— 覆盖 8 维度 ~75 用例，发现 7 项问题

- 测试基建：`tests/live-audit-round2-20260910.py`（endpoints/params/boundary/streamdeep/regress/sessioniso/auth/tooladv 八 section，分四批跑）。基线 UTC 12:49:01，闸 a12450c6（=93a5aeb）。
- **健康面**：鉴权矩阵 8/8（无 key/错 key/错 scheme/空 key/截断 key 全 401，GET 405，坏 JSON 400）；端点全覆盖通过——**Anthropic `/v1/messages`（非流式 contract + 流式 message_start/content_block_delta/message_stop 事件名）**、`/v1/responses` 五事件全序、`/v1/sessions`、`/v1/mcp/tools`(53 工具) 均正常；边界 15/16；流式首字符不丢 3/3、4 路并发零串扰、中断可恢复、长流尾完整；**regress 六项全过（上轮 93a5aeb 修复稳固）**；重负载大幅改善（48KB 15s、201 条消息 11s、60KB 24s，上轮 300KB 曾 71s）。
- **缺陷 1（P1，稳定复现）：`tool_choice` 指定函数名不生效。** body `{"type":"function","function":{"name":"get_time"}}` 连续 3 次 + Anthropic `{"type":"tool","name":"get_time"}` 均不产生 tool_use（finish=stop）。`tool_choice=none/required` 正常，`normalizedToolChoiceMode` 正确产出 "named:X" 并注入 router prompt 的 `MODE: %s`，但 router 不遵守该硬约束。
- **缺陷 2（P1，架构性）：`max_tokens`/`max_completion_tokens` 被静默忽略。** `max_tokens=12` → 实际 completion_tokens=1551，finish=stop（永不为 length）。根因：`chathub.Request` 结构根本没有 MaxTokens 字段（上游 Copilot WS 协议不承载采样参数），`oaiReq.MaxTokens` 只在 Anthropic 转换路径被读。
- **缺陷 3（P1，同因）：`stop` 序列被静默忽略。** `stop:["STOPHERE"]` → 回复 `'AAA STOPHERE BBB'` 未被截断。`oaiReq.Stop` 字段解析后全仓库无使用点（`chathub.Request` 亦无对应字段）。`temperature/top_p/frequency_penalty/presence_penalty/seed/n` 同样被解析后丢弃（temperature=0/2/99 均 200 无效果）。
- **缺陷 4（P2）：`stream_options.include_usage=true` 时真实增量流式路径不发 usage 帧。** 收尾帧只有 `x_m365_scores`/`x_m365_throttling`。`shouldSendStreamUsage()` 仅在 `writeToolResponse`（工具响应、伪流式）被调用；逐 delta 流式路径从未发 usage。
- **缺陷 5（P2）：上游空完成被归为 502。** `[upstream-fail] status=502 category=UPSTREAM_STRUCTURED err=upstream returned empty completion; tone may be unavailable for this tenant`。触发用例是 prompt-injection 请求（"Ignore all previous instructions..."），疑上游内容安全返回空。502 会让客户端误判网关故障并重试，语义应为 200+空内容或 4xx。
- **缺陷 6（P2，设计边界需文档化）：仅传增量消息时无服务端会话连续性。** 受控实验：body `session_id`、HTTP header `X-M365-Session-Id`、body `session_key` 三种机制 **same-id recall 全部 LOST（答 NONE）**，new-id 均 clean（不串味）。相对照：**同 session_id + 每轮重传全量历史 → CONTINUOUS（答对）**。即上轮禁上游记忆后，连续性完全依赖调用方重传历史；"只传最后一条消息"的调用方会丢上下文（旧行为下曾错配到无关旧会话，答出早前测试 token LARCH-5）。结论：现有客户端（ZCode 每轮传全量）安全，此边界需在文档中明确。
- **缺陷 7（P3，环境）：账号 `34bf92d4-6021-4f59-a5f6-5d05ecc5e912` 持续 unhealthy。** `[probe] still_unhealthy err=upstream metering throttle: capability access denied`，每 ~60s 重试仍失败，流量已旁落到 abd08ac9。
- **脚本断言 bug（已修，非产品缺陷）**：①`/v1/memory/settings` 为 **PATCH-only**（GET/POST/DELETE 均 405，属正确行为）；②SSE 中 `: connected`、`: m365-metrics {...}` 是**合法注释行**（规范允许），原断言误判 malformed。
- 待决策：缺陷 1/4/5 可直接修；缺陷 2/3 需选择"网关侧模拟截断"或"明确不支持并文档化"；缺陷 6 属文档/客户端契约。
