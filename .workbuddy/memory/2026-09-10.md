# 2026-09-10

## 服务器日志巡检（服务 PID 266114，09-09 14:36:51 UTC 启动，代码版本 7cd9bb2 / 0.6.5）

统计窗口 10.7h，4724 行日志：200 请求，164×200 / 35×401 / 1×404，**0 panic、0 error、0 invalid_request、0 empty_model_response**（d7e3b1e 前的问题未复现）。

### P0 功能缺陷：router 决策被 ledger 去重静默清空
- 证据：`2026/09/10 01:14:24 [router-nocalls] id=e8b53df8 stream=true route_prompt_len=213194 parsed=true upstream_text="CALL_TOOL: Read({\"file_path\":\"D:\\NetPeek\\src\\NetPeek.App\\ui\\history-ui.js\"})"`
- 链路：`internal/web/server.go:2103` `calls = filterCompletedCalls(calls, ledger)` → `agent_ledger.go:195`，按 **name + 规范化参数** 严格匹配已执行记录 → 命中即丢弃 → `len(calls)==0` 走 nocalls 分支 → 降级到 answer 轮 → 客户端工具调用丢失、模型开始瞎编。
- 无 `[tool-validation]` 日志（0 条），排除工具名校验拒绝。
- 修复方向：只读幂等工具（Read/Glob/Grep/LS）不去重或允许重复一次；去重清空但 parsed 有调用时，应把「该调用已执行、结果见 EVIDENCE_LEDGER」注入 answer prompt，而不是静默降级。

### P1 性能
- **连接池 0 复用**：`reused=false` 137 次 / `true` 0 次，每次 `ws_dial_ms` 平均 393ms。启动时的 `[connpool] warmed connection` 全没用上。
- **conv-cache 133 miss / 0 hit**，每请求新建云会话（router 轮还会 `dropTransientConversation` 删会话）。
- **router prompt 208–213KB**：`[router-slim] upgrade_to_full reason=reference_hint` 每次命中（长对话必然含指代词）→ 单轮 router 12.5s / 17.2s，占端到端大头。

### P2 运维
- `M365_MASTER_KEY` 未设置 → `[security] WARNING` **1462 条**（每请求 × 账号数），refresh token 用内置公开 key 加密。
- **版本分叉**：服务器 = 7cd9bb2（0.6.5，含 197a07f Graph provisioning、722de0e 限流冷却 banner），本地 HEAD = 9e08377，**本地仓库完全没有 7cd9bb2 这条线**。改本地前必须先从服务器 bundle 同步，否则会回滚 0.6.5。
- 127.0.0.1 出现 1 次 `admin_login_failed detail=invalid password`（疑似服务器本地旧脚本用老密码）。
- 128 次 `messages=2 tools=0` 的 chat（prompt 99 字符，回复 "Hello, AxonHub!"）为渠道健康检查，约每 5 分钟一次，每次真打上游。
- 耗时：http p50 5.99s / p90 8.42s / max 28.7s；chat p50 6.75s / p90 8.94s。
